[project]
name = "linux-access-revocation-audit"
version = "0.1.0"
description = "Audit Linux SSH access revocation after provider offboarding"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
